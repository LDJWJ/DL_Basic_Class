10/30
- 비지도학습(tsne)
- 비지도학습(군집, DBSCAN)


(1) tsne에 대해 배움.
( x ) tsne는 지도학습에 데이터 참고를 위해 사용하기 위한 알고리즘이다.

tsne는 언제 사용하는가? 시각화, 탐색 

(2) 군집 - knn 
( x ) knn은 비지도학습이다. 
* knn은 지도학습이다. 

(3) 군집 - kmeans 
( o ) kmeans는 비지도학습이고 군집으로 구분된다.

(4) 군집 - DBSCAN 
( x ) DBSCAN는 우리가 클러스터링 되는 개수를 직접 정할 수 있다.

* eps(거리와) min_sample를 정한다. 이를 통해 자연스럽게 클러스터링 개수가 정해진다.
* 임의지정을 하는 것은 K-means이다.

(5) 군집 - DBSCAN 
( x ) eps거리가 커지면 커질수록 클러스터링 개수는 많아질 것이다. 다른 매개변수는 고정되어 있다.



